app:
  name: safety-cctv-ai-model
  stage: train

data:
  train_dir: datasets/train
  val_dir: datasets/val
  class_map: configs/model_class_map.yaml

model:
  arch: yolo
  weights: ""
  img_size: 640
  batch: 16
  epochs: 50
  device: auto

output:
  exp_dir: experiments
  run_name: "train_baseline"
